# Data Products v1.7.0 Publication

Date: 2026-07-26

## Published identity

- GitHub Release: `data-products-v1.7.0`
- Release ID: `360090447`
- Published at: `2026-07-26T18:40:33Z`
- Exact tag target and source commit: `99e0e19b86cad6eae619f37702464e6a5a761cd8`
- Source Pull Request: `#275`
- Verified Pull Request head: `f97e732add506400b9e47ee95e59a3caf8382daf`
- Quality workflow: `#1934`
- Preflight Pull Request: `#276`, run `30215046551`
- Publication Pull Request: `#277`, run `30215200600`
- Independent audit Pull Request: `#278`, run `30215332217`

The tag resolves exactly to the squash-merged `main` commit above. The release is final, not a draft and not a prerelease.

## Public assets

| Asset | Asset ID | Size | SHA-256 |
| --- | ---: | ---: | --- |
| `SHA256SUMS` | `490591907` | 213 bytes | `ceeb2ab708984d6325bc0dacd6a4ba0acd3b750a5c5612f3ea8806bf55540a60` |
| `dacia-knowledge-base-data-products-v1.7.0.zip` | `490591898` | 62,077,663 bytes | `4bed3488d3cf0d175583c3e437bef83720962cb1c4162861a69e15e0aae4b67a` |
| `data-product-release-manifest.json` | `490591893` | 20,098 bytes | `1b5f24a1de919cce5fc87ec5c5406b04bf61052cd48e095439094d41d07e5e24` |

GitHub's recorded API digests match the independently calculated SHA-256 values for all three downloaded assets.

## Release contents

- 72 active configurations;
- 19 independent and comparable reporting scopes;
- 114 within-scope configuration pairs;
- 1,695 recorded differences;
- 83 deterministic archive members;
- JSON, Markdown, CSV, HTML and XLSX products;
- 124 source-backed technical comparison facets;
- 110 equipment facets;
- five official Dacia Polska model-media entries with offline SVG fallbacks.

Every active configuration belongs to exactly one scope. The release generates no cross-scope pair, ranking, recommendation or inferred value.

## Published-browser audit

The exact `shortlist/configuration-shortlist.html` member was extracted from the public archive after a fresh download. The embedded catalog contains 72 configurations, 124 technical comparison facets, 110 equipment facets and five official model-media entries.

The independent audit confirmed the differences-only view, equipment-list search, minimum and maximum price labels, image fallback, the CSS `hidden` override, `selection_conflict`, `addable_equipment` and the user-facing rule that selected filters are not removed automatically. The two legacy automatic-removal code paths remain absent.

## Offline workspace audit

The public downloader materialized the release into the canonical read-only workspace and the independent verifier accepted it:

- three canonical assets;
- 83 verified content files;
- 83 local links in deterministic `index.html`;
- workspace index SHA-256 `121284cf6578aece7fa35381ebd35945de9e20512dae11f474891a75106abd79`;
- exact release tag, source commit and snapshot date `2026-07-24`.

## Verification

All 15 source Pull Request workflows passed on the final clean source head, including 971 tests, Python 3.10, Python 3.13, the full Python 3.14 quality and artifact gate, Windows, HTML, workbook, comparison, selection-export and versioned-release verification.

The preflight rebuilt the candidate twice from the exact merged `main` commit, proved byte identity, verified both candidates and established the final sizes and SHA-256 values before publication. The publication workflow rebuilt the same candidate, required those exact identities, created the tag and release, and verified the public target and three-asset set.

Finally, an independent workflow downloaded the three public assets again, recalculated their hashes, ran the full release verifier, materialized and verified the public offline workspace and audited the exact published browser payload. Publication audit result: `PASS`.

## Data boundary

This release publishes the current evidence-backed repository state. It does not resolve deferred Duster 4x4 dimensions, ambiguous Jogger mass evidence or missing exact configurations. Missing evidence remains unknown rather than unavailable, and no comparison crosses an independent reporting scope.

## Immutability

The release assets and tag are immutable. Later corrections, source-backed additions or interface changes must use a new semantic version and must not replace or rewrite `data-products-v1.7.0`.
