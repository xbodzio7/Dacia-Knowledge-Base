# Data Products v1.12.0 Publication Trigger

This one-use file authorizes publication of `data-products-v1.12.0` from the exact merge commit containing this trigger, after all Pull Request checks pass.

The publisher must build twice, compare byte for byte, verify the offline workspace and verify publicly downloaded assets before recording completion.
