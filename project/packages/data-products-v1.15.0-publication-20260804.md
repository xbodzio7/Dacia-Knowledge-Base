# Data Products v1.15.0 Publication

Date: 2026-08-04

## Result

The immutable `data-products-v1.15.0` release was published from exact source commit `5a824010921ef305b481f8284ae45bdfd8801780`.

The release adds the verified portfolio source coverage matrix in JSON, CSV and standalone HTML. The offline consumer workspace exposes the direct `source_coverage_matrix_html` entry point and dedicated **Source coverage matrix** card alongside the existing family-summary, family-comparison and model-version entry points and cards.

The assets were built twice and were byte-identical. Both builds passed canonical verification, the complete extracted offline workspace passed verification, and the publicly downloaded assets matched the verified build byte for byte.

The public `data-products-v1.14.1` release remains immutable. No source data, master data, reporting scope, source quality score, source ranking, configuration pair, cross-scope pair, recommendation or inferred value changed.

## Assets

- `dacia-knowledge-base-data-products-v1.15.0.zip`;
- `data-product-release-manifest.json`;
- `SHA256SUMS`.

Exact sizes and SHA-256 values are stored in `data/reporting/data_products_v1_15_0_publication.json`.

## Next package

`post_v1_15_0_release_priority_selection_review_001` selects one bounded next package from canonical repository evidence.
