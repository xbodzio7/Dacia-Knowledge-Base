# Bigster Equipment Page 21 Unresolved Review — Chunk 1

## Package

- package: `residual_gap_028`;
- source: `src_pl_bigster_brochure_20251210`;
- model: `bigster`;
- domain: `equipment_matrix`;
- page: 21;
- candidates: 40 of 107;
- chunk: 1 of 3;
- evidence signatures: 0;
- evidence records: 0.

## Authored decision

The first 40 candidates are grouped into 20 visual equipment rows spanning exterior design, wheel choices and the first interior-design colour row.

Twenty candidates contain either a complete labelled row or an aligned availability/colour fragment and are classified as `unresolved_signature_mismatch`. They remain unresolved because the prioritized package contains no conservative exact evidence signature.

Twenty candidates are headings, wrapped labels, colour continuations or wheel-name continuations and are classified as `context_only_non_import`. They are necessary to reconstruct their visual rows but cannot form independent equipment or availability records.

## Grade and symbol boundary

The source prints four ordered grade columns:

1. `Essential`
2. `Expression`
3. `Journey`
4. `Extreme`

The symbols remain semantically distinct:

- `•` — standard;
- `¤` — option;
- `-` — unavailable.

No symbol is projected to another grade, and marker-only lines remain attached to the preceding or following wrapped label.

## Multi-line rows

The review preserves the following as single visual rows rather than independent candidate records:

- exterior-mirror colours;
- front/rear Dacia logo colours;
- Starkle® lower protection;
- lower bumper-cover colour and availability;
- copper-coloured grille details and wheel-centre caps;
- two-tone metallic paint with black roof and standard mirrors;
- two-tone metallic paint with black roof and copper-coloured mirrors;
- 18-inch half-diamond Tagasan wheels;
- 19-inch black diamond Rasan wheels;
- vent-surround colours.

The source literal `brązowomiedzinymi` is retained without correction.

## Five-package milestone review

This package completes the interval containing `residual_gap_024`, `residual_gap_025`, `residual_gap_026`, `residual_gap_027` and `residual_gap_028`.

The completed work remains within the existing `technical_tables` and `equipment_matrix` review model. It introduces no new domain, schema, architecture or import-policy decision. A separate review-only Pull Request is not required; work should continue with the next prioritized residual-gap package.

## Result

- 20 `unresolved_signature_mismatch`;
- 20 `context_only_non_import`;
- 0 selected evidence signatures;
- 0 selected evidence records;
- no master-data changes;
- no approved import specifications.

The queue advances to **Bigster Equipment Page 21 Unresolved Review — Chunk 2** (`residual_gap_029`, 40 of 107 candidates).
