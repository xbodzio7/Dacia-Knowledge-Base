name: Data Products v1.12.1 Visible PR Publisher

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  publish-data-products-v1-12-1:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/data-products-v1-12-1-visible-trigger-007' &&
      github.event.pull_request.base.sha == '6f208ecef49304c2f4bd9c6d46d93074b33aff6f'
    concurrency:
      group: publish-data-products-v1-12-1-pr-target
      cancel-in-progress: false
    timeout-minutes: 30
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out exact publication source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 0
          persist-credentials: true
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.14"
      - name: Publish and record Data Products v1.12.1
        env:
          GH_TOKEN: ${{ github.token }}
          PUBLICATION_SOURCE_SHA: ${{ github.event.pull_request.base.sha }}
          PUBLICATION_PR: ${{ github.event.pull_request.number }}
        run: bash tools/publish_data_products_v1_12_1_20260803.sh
