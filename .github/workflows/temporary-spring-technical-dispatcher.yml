name: Temporary Spring Technical Dispatcher

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

jobs:
  materialize:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dispatcher staged; waiting for complete package sources."
