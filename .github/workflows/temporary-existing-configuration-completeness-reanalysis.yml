name: Temporary Existing Configuration Completeness Reanalysis

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

concurrency:
  group: temporary-existing-configuration-completeness-reanalysis-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  materialize:
    if: github.event.pull_request.head.ref == 'agent/existing-configuration-completeness-reanalysis-20260801'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.14'
      - name: Install PDF backend
        run: |
          if ! command -v pdftotext >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install --yes --no-install-recommends poppler-utils
          fi
      - name: Materialize analysis
        run: |
          python tools/existing_configuration_missing_data_analysis.py
          python tools/dkb.py project-state --apply
          python tools/dkb.py documentation-baseline --apply
      - name: Check deterministic outputs
        run: python tools/existing_configuration_missing_data_analysis.py --check
      - name: Run package tests
        run: python -m unittest -v tests.test_existing_configuration_missing_data_analysis
      - name: Run full quality gate
        run: python tools/dkb.py quality --concise --database "$RUNNER_TEMP/dkb.sqlite" --log-file "$RUNNER_TEMP/quality.log" --summary-json "$RUNNER_TEMP/quality-summary.json"
      - name: Capture quality diagnostics
        if: failure()
        run: |
          if [ ! -f completeness-quality-diagnostic.txt ]; then
            {
              echo '=== QUALITY LOG ==='
              cat "$RUNNER_TEMP/quality.log" 2>/dev/null || true
              echo '=== QUALITY SUMMARY ==='
              cat "$RUNNER_TEMP/quality-summary.json" 2>/dev/null || true
            } > completeness-quality-diagnostic.txt
          fi
      - name: Commit quality diagnostics
        if: failure()
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f completeness-quality-diagnostic.txt
          if git diff --cached --quiet; then
            echo 'Quality diagnostics already captured.'
            exit 0
          fi
          git commit -m 'chore(ci): capture completeness quality diagnostics'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
      - name: Commit package
        run: |
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --all
          if git diff --cached --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git commit -m 'feat(reporting): reanalyze existing configuration completeness'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
