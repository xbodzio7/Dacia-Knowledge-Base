name: Jogger Injection Unit Diagnostic

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v7
        with:
          ref: data/jogger-injection-type-import
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Capture unit-test result
        shell: bash
        run: |
          set +e
          python -m unittest discover -s tests -p "test_*.py" -q \
            > project/reviews/jogger-injection-unit-diagnostic.log 2>&1
          status=$?
          rm .github/workflows/jogger-injection-unit-diagnostic.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: capture Jogger injection unit diagnostic"
          git push origin HEAD:data/jogger-injection-type-import
          exit "$status"
