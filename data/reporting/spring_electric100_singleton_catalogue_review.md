# Spring Electric 100 Singleton Catalogue Review

Date: 2026-07-31

Status: **complete**

## Exact source-backed state

The registered official Spring configurator snapshot proves one exact current Electric 100 passenger state:

- version: **Extreme**;
- transmission: **automatic**;
- catalogue price: **85,900 PLN**;
- observation date: **2026-07-31**.

The version, configuration, price and direct source relationships are import-ready.

## Singleton product contract

Electric 100 is not combined with the two Electric 70 configurations. It receives its own scope `spring_electric100_automatic` containing exactly one configuration.

The scope therefore has:

- one configuration;
- zero within-scope pairs;
- zero comparison differences;
- no cross-scope pair generation;
- no ranking, recommendation or inference behavior.

The singleton remains visible in the shortlist, workbook, offline workspace and cross-model navigation. Product tooling already supports singleton groups, so no new architecture is required.

## Expected repository result

The implementation will add one version, one configuration, one catalogue price, three direct source relationships and one completeness scope. The repository will then contain 81 active configurations, 22 reporting scopes, six model families and the unchanged total of 130 within-scope pairs.

## Deferred evidence

Technical values, equipment, packages and exact stock cards remain separate packages. The singleton foundation will not flatten Electric 70 and Electric 100 evidence or derive promotional prices.

Decision: `SELECT_SPRING_ELECTRIC100_SINGLETON_CATALOGUE_FOUNDATION`.
